# bookmark

Bookmark Hub — a static bookmark manager. Data now syncs to a Google Sheet
instead of GitHub. See `google-apps-script/SETUP.md` for the one-time setup.

`bookmarks.json` is no longer used by the app (kept only as a reference for
the old data shape) — you can delete it.